{
  "name": "React (openEuler)",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "..",
    "args": {
      "TAG": "latest"
    }
  },
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "forwardPorts": [3000, 5173],
  "portsAttributes": {
    "3000": {
      "label": "React App",
      "onAutoForward": "notify"
    },
    "5173": {
      "label": "Vite Dev Server",
      "onAutoForward": "notify"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode"
      ]
    }
  },
  "postCreateCommand": "if [ -f package-lock.json ]; then npm ci; elif [ -f yarn.lock ]; then corepack enable && yarn install; elif [ -f pnpm-lock.yaml ]; then corepack enable && pnpm install; elif [ -f package.json ]; then npm install; fi"
}
