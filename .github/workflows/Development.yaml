name: Deploy Next.js to GitHub Pages

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: 'pages'
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ./zydus-bill-view/package-lock.json

      - name: Install dependencies
        working-directory: ./zydus-bill-view
        run: npm ci

      - name: Build and export Next.js (static)
        env:
          # Provide the public API URL to the client build
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
        working-directory: ./zydus-bill-view
        run: |
          # `next.config.ts` sets `output: 'export'` and `distDir: 'out'`.
          # Run the package script that builds/exports static files.
          npm run export

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './zydus-bill-view/out'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4